// если подтвержден
const indicator=document.getElementById('email-indicator');
indicator.style.opacity='0';

const pannel = document.getElementById('user-pannel');
pannel.style.borderColor='#16162D'

//по дефолтику он не подтвержден